# Append-only logs: keep BOTH sides on a merge/rebase instead of
# conflicting. The Mac Mini pipeline relies on this to auto-recover when a
# push is rejected (see mini_cycle.py publish()).
*.jsonl merge=union
