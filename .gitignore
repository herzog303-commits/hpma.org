# OS / editor cruft
Thumbs.db
Desktop.ini
.DS_Store
*.swp
*~

# Local tooling / scratch (not part of the site)
/scratchpad/

# Scrape of the old hpma.org site — kept locally for reference, not deployed.
# Remove this line if you'd rather version-control the archive too.
/_original-site/

# wu.py falls back to a local secrets.json in its own dir -- this repo is PUBLIC,
# so make certain one can never be committed.
secrets.json
**/secrets.json
